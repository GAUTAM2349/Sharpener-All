* Dev dependency is use to install packages only in development environment.
* nodemon is used for live server
* package.json is a package manager, which helps to manage packages, and also install all packages in one command, 
  so no need to install them manually. It stores meta-data and scripts to execute.

* npm means node package manager, it is default package manager in node.js, we can use other also like yarn.

